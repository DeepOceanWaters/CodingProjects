# This is the main program
# Purpose is to create a randomly generated Dungeons and Dragons character
# This includes: name, race, class, attributes (e.g. STR/DEX/etc.)

def main:
    # add race OR replace races
    # add class OR replace classes
    # add names OR replace names
    # determine how attributes are set:
    #   - use array
    #   - use random
    #   - use point buy system
    #
    # call: createCharacter
    # call: printCharacter
    # return

def createCharacter:
    # determine name
    # determine class
    # determine race
    # set attributes
    #   - determine best use of attributes

def printCharacter:
    # outline:
    # [name] the [race] [class]
    #   [attribute.name]: [attribute.value] # Loop
    #
    # return
